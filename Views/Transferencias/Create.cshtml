@model Cooperativa.Models.Transferencias

@{
    Layout = "~/Views/Shared/AdministradorLayout.cshtml";
}

@{
    ViewData["Title"] = "Create";
}

<h1>Crear</h1>

<h4>Transferencias</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @* <label asp-for="cantidadTransferencia" class="control-label"></label> *@
                <label>Ingrese la cantidad a transferir:</label>
                <input asp-for="cantidadTransferencia" class="form-control" placeholder="Ejemplo: 225.30"/>
                <span asp-validation-for="cantidadTransferencia" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>FechaTransferencia</label>
                <input type="text" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss")" readonly />
                @Html.HiddenFor(m => m.fechaTransferencia, new { Value = DateTime.Now }) <!-- Campo oculto para la fecha -->
            </div>
            <div class="form-group">
                @* <label asp-for="detalleTransferencia" class="control-label"></label> *@
                <label>Detalle de la transferencia:</label>
                <input asp-for="detalleTransferencia" class="form-control" placeholder="Ejemplo: Transferencia pago a Iker Molina"/>
                <span asp-validation-for="detalleTransferencia" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* <label asp-for="cliente" class="control-label"></label> *@
                <label>Seleccione al cliente:</label>
                @* <input asp-for="cliente" class="form-control" /> *@
                <select asp-for="cliente" class="form-control" asp-items="ViewBag.cliente">
                    <option value="" disabled selected>--- Seleccione el cliente ---</option>
                </select>
                <span asp-validation-for="cliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Crear" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar al menu principal</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
